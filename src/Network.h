#pragma once
#include "Layer.h"
#include <vector>
#include <Eigen/Core>

namespace Backprop
{
/*
class Network 
{
  private:

	std::vector <Layer> _layer_stack;
	std::vector <VectorXd> _input_stack;

  public:

	VectorXd propagate(VectorXd& input);
	VectorXd backpropagate(VectorXd& output, VectorXd& target);

	std::vector<VectorXd> input_stack;
	std::vector<VectorXd> sensitivity_stack;

}
*/
}
